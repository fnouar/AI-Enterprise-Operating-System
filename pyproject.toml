[project]
name = "ai-eos"
version = "0.1.0"
description = "AI-EOS implementation monorepo"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"
